# 随心一听开发日志

本文件是项目接手后的持续记录。每个大版本或同类功能批次均新增一节，保留版本、操作记录、Bug 与修复记录、验证结果和需求状态。

## 1.2.5 - 端到端音质识别（进行中）

- 2026-07-24：确认 OWW221 与 Enco Free4 的实时链路为 AAC 165 kbps CBR、48 kHz、16-bit、立体声。
- 2026-07-24：确认双方共同可选 Codec 仅 AAC/SBC；手表有 LDAC 但耳机不提供，耳机有 LHDC 但手表固件缺少 LHDC。
- 2026-07-24：确认 production 固件禁止应用修改 A2DP offload 属性。
- 2026-07-24：新增蓝牙 Codec 监听和播放页“源音质 / 耳机音质”双层显示。
- 版本文档：`docs/bugfixes/1.2.5.md`。

## 1.2.4 - 低端音量与 Wi-Fi 流量测量

- 2026-07-24：确认 Wi-Fi 已连接，RSSI 约 -48 dBm，链路速率 72 Mbps。
- 2026-07-24：新增音频总大小、Range 分块耗时、吞吐率和累计下载量日志。
- 2026-07-24：新增随系统 0-16 档变化的低端输出衰减曲线和 120ms 渐变；系统媒体音量仍为唯一控制源。
- 2026-07-24：真机测得最低非静音档输出增益 0.34375（约 -9.3 dB），0 档静音，最大档保持 unity gain。
- 2026-07-24：完整下载《断桥残雪》无损 FLAC 45.40 MB；174 个分块纯传输 37.21 秒，突发平均 9.76 Mbps。
- 2026-07-24：回归封面、完整歌词、实际音质、seek 和 MediaSession；ADB 随后断开，主观响度由佩戴试听确认。
- 版本文档：`docs/bugfixes/1.2.4.md`。
- 流量报告：`analysis/WIFI_TRAFFIC_REPORT_1.2.4.md`。

## 1.2.3 - 蜂窝网络现场诊断

- 2026-07-24：复现 LTE 下无法开始播放。应用到网易云 API 的 TCP 443 建连超时。
- 2026-07-24：确认 LTE 注册、APN、地址、路由和 DNS 均存在，应用没有计费网络或后台流量限制。
- 2026-07-24：从手表系统直接测试网易云、百度、QQ、OPPO 域名，IPv4/IPv6 均无法建立公网连接，排除播放器音质和缓存导致的单点问题。
- 诊断记录：`analysis/CELLULAR_DIAG_1.2.3.md`。

## 1.2.3 - 歌词几何居中、音量一致性与防回归

### 操作记录

- 2026-07-24：收到 1.2.2 当前歌词回正后置底反馈，建立独立 Bug 文档和版本保留规则。
- 2026-07-24：收到音量偏大、调节阶跃及界面/实际音量一致性反馈；真机确认 OWW221 `STREAM_MUSIC` 为 0-16 档，当前扬声器为 1/16，应用调节记录确实落到系统媒体流。
- 2026-07-24：构建、签名并覆盖安装 `1.2.3`，保留 `1.2.0-1.2.2` 历史 APK。
- 2026-07-24：以《断桥残雪》完成歌词中心、手动滚动回正、封面、音质、seek、MediaSession、菜单路由、登录资料和旧包状态回归。

### Bug 与修复记录

- 已修复：自动居中公式加入 TextView 顶部 compound padding，以完整逻辑歌词（含自动换行）为整体计算中心，并限制到实际可滚动范围。
- 已加固：网络 MediaSession 显式绑定本地 `USAGE_MEDIA`，播放器不添加额外增益，状态更新记录真实系统音乐档位。

### 验证结果

- 当前单行及多行歌词整体居中；触摸滚动停止三秒后回中。
- 网络封面、完整歌词、`Hi-Res→无损 FLAC · 1599k` 显示保持有效。
- seek 实测落点 `142505 / 227161 ms`。
- 音量弹窗与系统音乐流同步：`1/16 → 3/16 → 1/16`。
- 网易云登录及 `1242/1242` 我喜欢资料保留，随机池为 1230 首。
- 设置进入 `SettingActivity`，旧零度音乐包不存在，无应用崩溃。
- APK：`artifacts/build/suixinyiting-network-1.2.3.apk`。
- SHA-256：`1E5C0BE0C1DECD3B17B351A52C1547774FEAE8F8150408EFDA559E818F55F70A`。

## 用户需求总汇总

- 保留零度音乐母版的完整 UI 与本地功能，产品统一更名为“随心一听”。
- 接入网易云账号、歌单、实时音频、封面和完整歌词，不批量下载歌曲。
- 千首歌单可完整浏览；随机播放覆盖完整可播放曲库且一轮不重复。
- 统一我喜欢、队列、歌单、专辑、歌手菜单，不增加重复入口或破坏原路由。
- 网络歌曲共享母版播放页、进度、歌词、背景、播放控制、媒体通知和系统音量。
- 支持首选音质切换，并显示服务端实际音质、格式、码率和降档结果。
- 清除缓存仅删除临时音频与图片，不清理登录和资料库元数据。
- 歌词支持触摸及表冠滚动，停止手动滚动后自动将当前歌词居中。
- 进度条拖动需要流畅且实际改变网络歌曲播放位置。
- 审计设置页，修复仍适用于网络播放的功能；明确不适用或受系统条件限制的项目。
- 系统应识别当前媒体播放；评估系统应用内置条件，并移除旧零度音乐应用。
- 每个大版本或功能批次更新本日志。

## 1.2.2 - 播放交互、设置审计与系统集成

### 操作记录

- 2026-07-24：接手 `1.2.1` 源码、补丁树、签名文件和 OWW221/API 30 真机环境。
- 2026-07-24：确认当前设备为 production `user` 构建，ADB 序列号 `2e28bb17`，`adbd` 不支持 root。
- 2026-07-24：确认旧零度音乐包为 `ml.sky233.zero.music`，随心一听包为 `com.poyi.suixinyiting`。
- 2026-07-24：开始审计歌词回正、网络播放 seek、音量链路、设置入口与系统媒体会话。
- 2026-07-24：逐项真机打开本地扫描、界面、主题、文件传输、歌词设置、播放、帮助和关于页面，无 Java/Kotlin 崩溃。
- 2026-07-24：解除 native 初始化对旧 APK 上下文的依赖，并从真机卸载 `ml.sky233.zero.music`；卸载后冷启动与网络播放通过。
- 2026-07-24：确认 `SuixinNetwork` 为系统媒体会话，AudioService 音频焦点归属 `com.poyi.suixinyiting`。

### Bug 与修复记录

- 已修复：歌词手动滚动或表冠操作停止三秒后主动平滑回正；回正前后截图哈希一致。
- 已修复：网络进度条按绝对触摸位置平滑更新，松手调用 `MediaPlayer.seekTo()`；真机验证 `86,067 / 227,161 ms`。
- 已修复：移除网络播放器固定 65% 二次增益和重复音量菜单，统一使用母版/系统媒体音量。
- 已修复：撤销基于母版视图显示状态的错误背景判断，网络歌曲封面背景恢复稳定显示。
- 已修复：系统空闲母版 MediaSession 的 `Zero Music / Music player` 元数据改为“随心一听 / 音乐播放器”。
- 已修复：隐藏失效的旧在线更新、特别感谢、QQ群和关于开源入口，更新应用帮助、图标与副标题。

### 当前限制

- 将 APK 写入 `/system` 或 `/product` 需要解锁/工程固件、root 或厂商签名；当前 production user 系统不具备这些写入条件。系统媒体识别、媒体按键和系统音量控制已经通过 MediaSession/AudioService 实现，不依赖物理预装。
- 母版均衡器、压限器、左右声道增益以及高级歌词字体/翻译/逐字歌词尚未绑定网络播放链路，当前仍只对本地播放器或本地歌词组件生效。
- 母版纯黑、自定义、模糊和流体背景选项尚未显式桥接网络封面层；当前网络播放固定显示歌曲封面背景。

## 1.2.1 - 音质可见性与网络音量

### 操作记录

- 增加 Hi-Res、无损、极高、较高、标准五档首选音质。
- 播放页显示请求档位、实际档位、格式和服务端码率。
- 服务端同次请求直接降档时显示 `Hi-Res→无损`。
- 修复 Windows D8 命令行过长，先将 class 文件打包为 JAR 再生成 `classes2.dex`。

### Bug 与修复记录

- 修复低档音质选择仍走无损优先的问题。
- 修复音质弹窗触摸透传导致进入页面时误选第一项的问题。
- 真机实测曲目最高返回 `lossless / FLAC / 1,598,781 bit/s`，Hi-Res 请求对该曲目降为无损。

## 1.2.0 - 菜单与网易云资料库统一

### 操作记录

- 移除 `ConcatAdapter` 外挂菜单，复用母版我喜欢、队列、歌单、专辑、歌手入口。
- 数据库升级到 v3，保存专辑 ID、歌手 ID 与歌曲-歌手关系。
- 全歌单元数据聚合去重；合作歌曲分别归入每位歌手。
- 队列读取真实 shuffle bag，并保留历史、当前项和剩余顺序。

### Bug 与修复记录

- 修复菜单位置偏移导致“设置”跳转到我喜欢的问题。
- 修复重复播放队列和重复歌单入口。
- 1230 首随机池一轮无重复验证通过。

## 日志维护规则

- 每次版本升级先新增版本节，再开始大批次修改。
- 操作记录只写实际执行过的构建、安装、迁移和验证。
- Bug 条目在修复后补充实现和验证结果，不直接删除历史问题。
- 未满足的需求保留状态与原因，避免在后续迭代中丢失。
- 2026-07-24：开始 1.2.6，修复 Activity 重建/返回前台后错过状态广播造成的“未播放”和音质信息缺失；增加播放页主动状态请求。
- 2026-07-24：开始 1.2.7；1.2.6 真机截图验证源音质已恢复但耳机 Codec 仍为空，增加服务冷启动时读取当前 A2DP 粘性 Codec 状态。
- 2026-07-24：开始 1.2.8；压缩播放页音质行，并在音质选择框完整展示源与耳机实际规格，解决小屏参数裁切。
- 2026-07-24：开始 1.2.9；真机发现 OPPO AlertDialog 标题覆盖单选列表，改为单层自适应行高音质面板。
- 2026-07-24：1.2.9 真机验收完成：资料库 1242 首、随机池 1230 首；《断桥残雪》实际源为无损 FLAC 1598.781 kbps，Enco Free4 为 AAC 165 kbps / 48 kHz / 16-bit；seek 到 181306/227161 ms 后 MediaSession 与歌词同步；后台返回正常，无 FATAL EXCEPTION。截图保存在 `artifacts/build/verify-1.2.9-player.png`、`verify-1.2.9-dialog.png`、`verify-1.2.9-return.png`。

## 2026-07-25 - 长期文档基线

### 需求范围

- DOC-003：将全部历史对话、需求、Bug、实现边界和验证方式整理为可长期接手的规范文档。

### 操作记录

- 新建 `docs/INDEX.md`，作为规范、需求、架构、开发、测试、Bug、发布和日志的唯一导航入口。
- 将历史需求整理为稳定 ID，明确优先级、完成/部分完成/待开发/环境受限状态和验收条件。
- 建立 25 项跨版本 Bug 总表，补充根因、修复版本和固定回归锚点。
- 固化 bridge/Smali 架构边界、多设备 ADB、签名隔离、数据迁移、版本保留和 GitHub 发布规则。
- 建立 Smoke/Core/Extended/Soak 测试矩阵和版本日志模板。

### 结果

- 应用代码与 APK 版本保持 `1.2.9`，本批次只建立文档治理基线，不改变运行行为。
- 后续开发必须先更新需求和版本文档，再实施代码，真机验证后才能标记完成。

## 1.3.0 - 播放页与歌词页性能优化

### 需求范围

- NFR-007/NFR-008/NFR-009：降低页面切换和歌词滚动卡顿，同时减少无效广播、布局、纹理内存与暂停轮询。

### 基线

- 2026-07-25：1.2.9 真机 `gfxinfo` 178 帧中 102 帧卡顿（57.30%），P95 150 ms，missed vsync 43。
- 2026-07-25：发现服务每秒携带整份歌词，Bridge 每秒重建 Spannable/布局/平滑滚动，封面以 2048x2048 原图解码并重复绑定多个 View。
- 2026-07-25：完整歌词改为仅在换歌、解析完成和主动请求时发送；ticker 只发送进度与当前行，歌词索引改为二分查找。
- 2026-07-25：歌词按 revision 缓存，仅当前行变化时创建三个 Span；手动滑动期间延迟高亮和自动滚动，停止三秒后一次性渲染并回中。
- 2026-07-25：封面从 2048x2048 按 sample 4 解码为 512x512 RGB_565，相同 Activity/Bitmap 不重复绑定；固定 View 样式不再每秒重设。
- 2026-07-25：暂停停止 ticker，10 秒观察 CPU 0%、新增渲染帧 0；播放页退到后台也停止 UI ticker，8 秒后台播放 CPU 0%，回到前台完整状态立即恢复。
- 2026-07-25：最终 APK 代表性场景为 576 帧中 220 janky（38.19%），P95 32 ms、missed vsync 1；稳定 PSS 41,487 KB、Graphics 6,100 KB。ADB 注入产生 449 次 high input latency，前一轮热态卡顿率为 26.86%。
- 2026-07-25：登录、1242/1242 资料库、1230 随机池、无损播放、封面、完整歌词、三秒回中和 seek 181306/227161 ms 回归通过。
- APK：`artifacts/build/suixinyiting-network-1.3.0.apk`；SHA-256：`DB2E9B8843D33EFB85F151139F2B1C42780A75428484C8ADE5452118B38BF06B`。
- 版本文档：`docs/bugfixes/1.3.0.md`。

## 1.5.0 - 手表 UI 重构与系统媒体音量归一

### 需求范围

- UI-005/UI-008：播放页、歌词页、音量面板、菜单和资料库按 378x496 手表屏重构。
- AUDIO-004：系统媒体音量成为唯一控制源，界面与实际响度一致。
- NFR-007/NFR-008/NFR-009：切页、歌词滚动和列表滚动流畅，稳态省电。
- ENV-001：手表网络 ADB 常驻。

### 基线

- 2026-07-26：真机 `dumpsys media.audio_policy` 读到 OWW221 的 `STREAM_MUSIC`
  耳机曲线为 `(1,-58dB) (20,-40dB) (60,-17dB) (100,-2dB)`；应用另加
  `index/max` 线性增益构成二次衰减，A2DP 3/16 档由 -41 dB 掉到约 -55 dB。
- 2026-07-26：确认 Activity 从未调用 `setVolumeControlStream()`，注入
  `KEYCODE_VOLUME_UP` 时 `STREAM_MUSIC` 不变。
- 2026-07-26：确认播放页传输键为 50/58dp，在 189dp 宽面板上占满整行；
  音质被塞进 `@id/artist` 并压到 8sp；无已播/总时长。
- 2026-07-26：确认资料库 `getView()` 忽略 `convertView`，1242 行每格重建视图树。

### 实现记录

- 2026-07-26：播放器恒定 unity 增益，仅保留防爆音渐入；新增
  `ACTION_ADJUST_VOLUME`/`ACTION_SET_VOLUME`，状态广播携带 `volume`/`volumeMax`。
- 2026-07-26：`MainActivity` 与 `NetworkMusicActivity` 设置
  `setVolumeControlStream(STREAM_MUSIC)`。
- 2026-07-26：重写 `fragment_activity_main_play.xml`、
  `fragment_activity_main_lyric.xml`、`dialog_volume.xml`，调整
  `activity_main.xml` 音量浮层与封面背板，缩放 `ItemMenuImage`/`ItemMenuTitle`。
- 2026-07-26：bridge 改为一次性解析资源 id、按 Activity 缓存视图；
  歌词高亮改为移动 `ForegroundColorSpan`；封面与歌词滚动容器改为读取 XML 节点。
- 2026-07-26：资料库改用 ViewHolder 复用，封面按 `?param=120y120` 取缩略图。
- 2026-07-26：资源改动集中到 `res-overlay/`，由 `patch_suixin.py` 的
  `patch_watch_ui()` 幂等覆盖，保证从干净反编译可复现。

### 真机记录

- 覆盖安装 `versionCode 10500 / versionName 1.5.0`，登录与 `1242/1242` 保留。
- 应用内滑条把 `STREAM_MUSIC` 由 `5` 调到 `9`，日志恒为 `output gain=1.0 (unity)`。
- 播放页 20 秒稳态：20 帧、`Slow UI thread 1`、P95 34ms、Missed Vsync 0；
  修复每秒重新布局前为 `Slow UI thread 11`、P95 44ms。
- 资料库滚动：561 帧、`3.92%` janky、P50 5ms、P95 13ms、Missed Vsync 0；
  加入行复用前为 `8.39%` janky、P95 17ms。
- 暂停 20 秒新增渲染帧 0；无 `FATAL EXCEPTION`。
- APK SHA-256：`762167FBD1717AB37A677BC7DD99C6B9FB12F1EAA0DD3E0F67B8D0F341EC8FF8`。
- 版本文档：`docs/bugfixes/1.5.0.md`。

### 环境与限制

- 手表为 production `user` 构建：`setprop persist.adb.tcp.port` 被拒绝，
  `settings put global adb_wifi_enabled 1` 被框架回滚为 0，网络 ADB 无法写进手表。
  常驻改由 PC 侧 `scripts/watch_adb_keepalive.ps1` 保活，掉线自动重连，
  接上 USB 时自动重新 `adb tcpip 5555` 并回读 wlan0 地址。
- 歌词页独立 `gfxinfo` 场景未采到：测试期间手表运行「自由记录」运动会话，
  系统层抢占横向手势，注入翻页多次被切到系统应用。
- 表冠旋转无法用 `adb input` 注入 `AXIS_SCROLL`，表冠调音与滚动歌词需佩戴实测。

## 1.4.0 - 持久缓存与播放稳定版（进行中）

### 需求范围

- NET-003/005/006、PLAY-011/012、NFR-010：持久块缓存、自适应预取、严格 LRU、播放 generation、耳机断连和安全恢复。
- BUG-008 复发、BUG-027 至 BUG-029。

### 基线

- 2026-07-25：确认 1.3.0 缓存块位图只存在内存，文件名仅使用 trackId，服务重启后不能可靠复用；淘汰只在新建数据源时执行。
- 2026-07-25：确认异步播放没有统一 generation，旧 MediaPlayer.onPrepared 可在快速切歌后抢播。
- 2026-07-25：确认网络设置已有蜂窝高音质提示和独立清缓存入口，可在原入口内扩展而不增加菜单。
- 目标版本：`1.4.0 / 10400`；默认缓存 512MB；上一稳定版 `1.3.0` 保留。
- 版本文档：`docs/bugfixes/1.4.0.md`。

### 实现记录

- 2026-07-25：新增 `AudioCacheKey`、`AudioCacheStore`、`CacheStats`、`AudioPrefetchManager` 和 `PlaybackPhase`；缓存数据库升级到 v2。
- 2026-07-25：缓存改为 256KB 持久块，写入 sync 后提交位图；校验 206/Content-Range/长度/字节数，并实现完整/部分缓存读取与低存储直连。
- 2026-07-25：写入前严格预留容量，活动项和写入项 pin；清理改为独立维护线程，待释放标记写入数据库并在进程重建时清理。
- 2026-07-25：预取采用单线程及播放优先下载门控，加入 Wi-Fi/蜂窝/充电/电量/省电回调与首数、时间、字节三重预算。
- 2026-07-25：播放服务加入 generation、七阶段状态、两工作线程最新请求队列、15 秒检查点、耳机路由恢复和 `ACTION_AUDIO_BECOMING_NOISY`。
- 2026-07-25：播放页恢复时比较实际 Drawable；设置页整合音质、预取、网络策略、容量、统计和清理；系统音量增益改为线性映射。

### 构建与真机记录

- bridge javac/D8、apktool、zipalign、apksigner 均通过；APK v1/v2/v3 签名有效。
- 有线覆盖安装到 `2e28bb17` 成功，设备报告 `versionCode 10400 / versionName 1.4.0`；登录和 `1242/1242` 资料库保留。
- 同曲缓存 `23,592,960` 字节跨安装和服务重建复用；完整缓存曲目日志为 `complete=true / offline=true`。
- 清缓存立即释放 `227,926,494` 字节，当前项 `25,427,968` 字节延迟；进程重建后同曲变为 `cached=0`，资料库仍为 `1242/1242`。
- 无耳机服务恢复到 `209690ms` 且保持暂停；系统 MediaSession、标题、歌手和实际音质一致。
- 后台返回截图按钮为两竖暂停图标；性能为 janky `31.76%`、P95 `27ms`、missed vsync `0`、PSS `45,399KB`。
- 静态复核后将预取任务队列限制为 1，设置策略变更立即取消/重排，容量下调立即收缩；新增清理失败字节并隔离新曲待恢复位置。
- 最终 APK SHA-256：`48DBA42D2B4650E3E572B2216F46A818DF99AA60B0C5920B8EB6063302943A76`。
- 真实耳机断连、物理断网、满容量 LRU、蜂窝/低电量策略仍按版本文档列为现场复测，不记为通过。
- 2026-07-26 00:06:06 至 00:11:07 完成 5 分钟短时后台检查：PID `10069` 保持不变，PSS 从 37,135KB 到 42,546KB，无应用 FATAL EXCEPTION 或 ANR；30 分钟测试按用户要求停止。结束后播放已暂停在 `97,930ms`，系统音乐音量恢复为 `5/16`。
