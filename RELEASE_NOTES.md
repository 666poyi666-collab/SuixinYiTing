# 随心一听 1.5.0

## 更新

- 播放页按 378x496 手表屏重建：新增可读性蒙版、居中标题与歌手、独立音质行、
  已播/总时长，传输键由 50/58dp 缩到 36/52dp，底部键由 50dp 缩到 30dp。
- 歌词页滚动容器写进布局，不再每次进入页面都重建视图；新增暗色蒙版，
  正文 13sp、行距 8dp，长句不再出现单字换行。
- 音量面板由占满整屏的滑条改为 26dp 轨道，并标明这是系统媒体音量；
  音量浮层改为贴近表冠的竖条。
- 菜单与资料库按手表密度重排，「我喜欢的音乐」不再被截断，
  单屏可见项分别由 3 增至 4 和 5。
- 系统媒体音量成为唯一控制源：移除播放器的二次线性增益，
  音量键与表冠固定作用于 `STREAM_MUSIC`，界面档位与实际响度一致。
- 渲染开销治理：每秒时钟不再触发全窗口重新布局，歌词换行只移动颜色 Span，
  资料库列表复用行视图并改用服务端缩略图。

## 验证（OPPO OWW221 / Android 11 / API 30）

- 覆盖安装 `versionCode 10500 / versionName 1.5.0`，登录与 `1242/1242` 资料库保留。
- 应用内音量滑条把系统媒体音量由 `5/16` 调到 `9/16`，日志恒为 `output gain=1.0 (unity)`。
- 播放页 20 秒稳态：`Slow UI thread 1/20`、P95 `34ms`、missed vsync `0`
  （修复前为 `11/20`、`44ms`）。
- 资料库滚动 561 帧：`3.92%` janky、P50 `5ms`、P95 `13ms`、missed vsync `0`
  （修复前为 `8.39%`、`17ms`）。
- 暂停 20 秒新增渲染帧 `0`；无 `FATAL EXCEPTION` 或 ANR。

## 仍需现场复测

- 表冠旋转调音与表冠滚动歌词：`adb input` 无法注入 `AXIS_SCROLL`，需佩戴实测。
- 歌词页单独的 `gfxinfo` 场景：测试期间手表运行运动会话抢占横向手势，
  性能以歌词页始终挂载状态下的稳态数据代替。
- 真实蓝牙/有线耳机断连、物理断网缓存播放、满容量 LRU 与低电量策略。

## 开发环境说明

手表为 production `user` 构建，`persist.adb.tcp.port` 与 Android 11 无线调试
均被固件拒绝，网络 ADB 无法写入设备。仓库提供 PC 侧保活脚本
`scripts/watch_adb_keepalive.ps1`：掉线自动重连，接上 USB 时自动重新
`adb tcpip 5555` 并回读 wlan0 地址，用法见 `docs/DEVELOPMENT_GUIDE.md` 6.1。

APK SHA-256：`762167FBD1717AB37A677BC7DD99C6B9FB12F1EAA0DD3E0F67B8D0F341EC8FF8`
